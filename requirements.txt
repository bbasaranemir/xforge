# NOTE: apache-airflow==2.8.1 is provided by the Docker image (Dockerfile.airflow).
# Scripts in this repo run inside the airflow-scheduler container.
# To run locally without Docker, install: pip install apache-airflow==2.8.1
statsbombpy==1.18.0
pandas==2.1.4
numpy==1.26.3
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
dbt-postgres==1.7.4
protobuf>=4.0.0,<5.0.0
scikit-learn==1.4.0
xgboost==2.0.3
scipy==1.12.0
mplsoccer==1.2.4
matplotlib==3.8.2
requests==2.31.0
joblib==1.3.2
# apache-airflow-providers-docker is provided by the Airflow Docker image at runtime.
# DAG files (dags/) are loaded by Airflow's scheduler, not run from this requirements file.
